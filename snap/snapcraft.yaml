name: monophony # you probably want to 'snapcraft register <name>'
base: core22 # the base snap is the execution environment for this snap
adopt-info: monophony
#icon: icon.svg
grade: stable # must be 'stable' to release into candidate/stable channels
confinement: strict # use 'strict' once you have the right plugs and slots
compression: lzo
    
parts:
  libexpat:
    source: https://github.com/libexpat/libexpat.git
    source-tag: R_2_5_0
    source-subdir: expat
    source-depth: 1
    plugin: autotools
    autotools-configure-parameters:
      - --prefix=/usr
      - --without-docbook
    override-prime: |
      echo 'Skip'
  zlib:
    source: https://github.com/madler/zlib.git
    source-tag: v1.2.13
    plugin: autotools
    autotools-configure-parameters:
      - --prefix=/usr
    override-prime: |
      echo 'Skip'
  monophony:
    after: [ libexpat, zlib, mpris-server ]
    # See 'snapcraft plugins'
    plugin: make
    source: https://gitlab.com/zehkira/monophony.git
    source-tag: v1.7.1
    source-subdir: source
    source-type: git
    stage-packages:
      - gir1.2-gstreamer-1.0
    build-environment:
      - PATH: ${CRAFT_STAGE}/bin:${PATH}
      - PYTHONPATH: ${CRAFT_STAGE}/lib/python3.10/site-packages:$PYTHONPATH
    override-pull: |
      craftctl default
      pip install nuitka
      sed -i 's|prefix = /usr|prefix=${CRAFT_PART_INSTALL}|' source/Makefile
      sed -i 's|python3 -m nuitka bin/monophony.py --remove-output --clean-cache=all --follow-import-to=monophony -o monophony.bin|python3 -m nuitka bin/monophony.py --remove-output --clean-cache=all --follow-import-to=monophony -o monophony.bin --static-libpython=no|' source/Makefile
    override-build: |
      pip install nuitka ordered-set
      export PATH=$HOME/.local/bin:$PATH
      craftctl default
    parse-info: [share/metainfo/io.gitlab.zehkira.Monophony.metainfo.xml]
    prime:
      - -usr/lib/x86_64-linux-gnu/glib*
      - -usr/lib/x86_64-linux-gnu/libgio*
      - -usr/lib/x86_64-linux-gnu/libglib*
      - -usr/lib/x86_64-linux-gnu/libgmodule*
      - -usr/lib/x86_64-linux-gnu/libgobject*
      - -usr/lib/x86_64-linux-gnu/libgthread*
      - -usr/share/doc/*
      - -usr/share/glib-2.0
      - -usr/share/lintian
      - -usr/lib/x86_64-linux-gnu/libgirepository*
      - -usr/lib/x86_64-linux-gnu/libdw*
      - -usr/lib/x86_64-linux-gnu/libunwind*
    organize:
      usr: .
  mpris-server:
    source: https://github.com/alexdelorenzo/mpris_server.git
    source-tag: 'v0.4.3'
    plugin: python
    build-environment:
      # WORKAROUND: The python plugin is broken with gnome extension
      - PATH: ${CRAFT_PART_INSTALL}/bin:${PATH}
      - PYTHONPATH: ""    
    prime:
      # WORKAROUND: Skip venv from python plugin
      - -bin/activate
      - -bin/activate.cshordered-set
      - -bin/activate.fish
      - -bin/activate.csh
      - -bin/Activate.ps1
      - -bin/python
      - -bin/python3
      - -bin/python3.10
      - -bin/pip
      - -bin/pip3
      - -bin/pip3.10
      - -pyvenv.cfg
      - -lib/python3.10/site-packages/gi
      - -lib/python3.10/site-packages/pygtkcompat
      - -lib/python3.10/site-packages/PyG*
      - -lib/python3.10/site-packages/pycairo*
      - -share
      - -include
    python-packages:  
      - requests
      - ytmusicapi
      - yt-dlp
      - lxml
      - MarkupSafe
      - appdirs
      - Jinja2
  cleanup:
    after: [ monophony, mpris-server ]
    plugin: nil
    override-prime: |
      set -eux
      cd $CRAFT_PRIME
      rm -rf lib/x86_64-linux-gnu/girepository-1.0/GIRepository-2.0.typelib lib/x86_64-linux-gnu/girepository-1.0/GLib-2.0.typelib lib/x86_64-linux-gnu/girepository-1.0/GModule-2.0.typelib lib/x86_64-linux-gnu/girepository-1.0/GObject-2.0.typelib lib/x86_64-linux-gnu/girepository-1.0/Gio-2.0.typelib lib/x86_64-linux-gnu/libunwind* lib/x86_64-linux-gnu/libdw* lib/x86_64-linux-gnu/libunwind* share/doc
apps:
  monophony:
    command: bin/monophony
    desktop: share/applications/io.gitlab.zehkira.Monophony.desktop
    extensions: [gnome]
    environment:
      PYTHONPATH: $SNAP/lib/python3.10/site-packages:$PYTHONPATH
    plugs:
      - home
    slots:
      - mpris
